# Never commit these. terraform.tfstate can contain sensitive values in
# plain text (the same lesson as Secrets, two classes ago), and
# terraform.tfvars is specific to your own AWS account.

.terraform/
.terraform.lock.hcl
terraform.tfstate
terraform.tfstate.backup
terraform.tfvars
*.tfplan
crash.log